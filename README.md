A deep learning-based traffic classification system that uses a CNN model to identify traffic conditions as **HEAVY** or **LOW** from images. 
Built with TensorFlow and OpenCV, with optional voice interaction and real-time prediction support.
